//Maybe change with INSA Cpp model file

int main()	{
	//Parse Parameters
	// const Automate
	// Start Automate (@Params[options,..., file])
	int error = AUtomate.init();
	switch(error)	{
		case:
		//......
		break;
		default:
		break;
	}

	return 0;
}
